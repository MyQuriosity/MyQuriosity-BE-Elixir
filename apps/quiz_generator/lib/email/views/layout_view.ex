defmodule QuizGenerator.Email.LayoutView do
  # credo:disable-for-next-line Credo.Check.Readability.NestedFunctionCalls
  use Bamboo.View, path: "#{:code.priv_dir(:quiz_generator)}/templates/layout"
end
